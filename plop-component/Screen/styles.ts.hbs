import { useMemo } from 'react';
import { StyleSheet } from 'react-native';

export const useStyle{{pascalCase name}} = () => {
  return useMemo(() => {
    const styles = StyleSheet.create({});

    return {
      styles,
    };
  }, []);
};
