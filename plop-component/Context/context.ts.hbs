import { createContext } from 'react';
import { {{pascalCase name}}Value } from './types';

const NOOP = () => {};

export const {{pascalCase name}}Context = createContext<{{pascalCase name}}Value>({
  show: NOOP,
  hide: NOOP,
});
